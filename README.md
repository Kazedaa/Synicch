# Synicch

A self-hosted photo library for one phone and one small home server.

Syncthing copies the camera roll to a machine you own; this indexes what
lands there and serves it to a native Android app.

- `server/` - Python: indexer, thumbnails, junk detection, JSON API
- `app/` - Android, Kotlin and Compose

Early days. Nothing here is finished yet.
